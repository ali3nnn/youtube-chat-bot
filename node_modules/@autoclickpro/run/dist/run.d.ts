import minimist from "minimist";
type Cmd = (option: Record<string, any>) => Promise<any> | any;
export type CmdMap = {
    [cmd: string]: Cmd | CmdMap;
};
export declare function run(cmdMap: CmdMap, ministOpts?: minimist.Opts): Promise<void>;
export {};
